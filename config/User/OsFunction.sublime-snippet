<snippet>
	<content><![CDATA[
public function Assoc $1(Object prgCtx,  $2 )
	Object		dbConnect = prgCtx.fDbConnect
	CAPICONNECT	dbConnection= dbConnect.fConnection
	DAPISESSION dSession = prgCtx.dSession().fSession
	Dynamic		result
	Assoc 		retval

	retval.ok = FALSE
	retval.errMsg = ''


	//code
	$3

	retval.ok = TRUE;
	return retval
end
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>OsFunction</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.os</scope>
</snippet>
